{
  "scripts": {
    "test:demo": "npm run test:3000 || npm run test:3001 || npm run test:3002",
    "test:3000": "set TEST_PORT=3000 && npx playwright test",
    "test:3001": "set TEST_PORT=3001 && npx playwright test",
    "test:3002": "set TEST_PORT=3002 && npx playwright test",
    "allure:generate": "npx allure generate allure-results --clean",
    "allure:open": "npx allure open allure-report",
    "allure:serve": "npx allure serve allure-results"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "allure-commandline": "^2.34.1",
    "allure-playwright": "^3.4.1"
  }
}
